# Copy to ".env" (or set in your host's dashboard).
# Base URL of the backend API. Leave blank for local dev (Vite proxies /api
# to http://127.0.0.1:8000). In production set it to your deployed backend,
# e.g. https://your-backend.up.railway.app
VITE_API_BASE_URL=
